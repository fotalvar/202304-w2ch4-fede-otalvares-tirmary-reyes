import CodersArray from "./CodersArray";

describe("Given a class CodersArray that simulates the array method lengths", () => {
  describe("When it receieves 1, 2, 3, 4", () => {
    test("Then it should return 4", () => {
      const receivedItems = 
      const expectReturn = 4;

      
    });
  });
});

